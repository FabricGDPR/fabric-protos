name: Push protobuf updates to fabric-protos-go

on: 
  push: 
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Push protobuf updates to fabric-protos-go
      uses: actions/checkout@v1
      with:
        key: ${{ secrets.SSH_KEY }}
      run:  make update
